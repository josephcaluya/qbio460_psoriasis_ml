A Random Forest model trained to classify skin samples as psoriatic or normal. Hyperparameters are optimized using grid search, and the top 10 gene probes are identified. The .txt file includes the samples, gene expression values, and locations. The .ipynb file contains all the code. The .csv file contains the labels.
